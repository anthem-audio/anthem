include "project.fbs";

table Exit {}
table Heartbeat {}

union Command {
  // Application commands
  Heartbeat,
  Exit,

  // Project commands
  AddGenerator,
}

union ReturnValue { }

table Request {
  id: int;
  command: Command;
}

table Response {
  id: int;
  return_value: ReturnValue;
}
